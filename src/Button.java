import javax.swing.*;
import java.awt.*;

public class Button
{
    public static JButton CreateButton(String text)
    {
        JButton button = new JButton(text);

        return button;
    }
}
